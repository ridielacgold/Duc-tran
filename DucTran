-- [[ GEMINI HUB - VERSION 3.0 FINAL ]] --
local Player = game.Players.LocalPlayer

-- Hàm tự động cầm vũ khí
local function EquipWeapon()
    pcall(function()
        local tool = Player.Backpack:FindFirstChild("Combat") or Player.Character:FindFirstChild("Combat")
        if tool and not Player.Character:FindFirstChild(tool.Name) then
            Player.Character.Humanoid:EquipTool(tool)
        end
    end)
end

-- GIAO DIỆN
local ScreenGui = Instance.new("ScreenGui", game.CoreGui)
local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 200, 0, 100)
MainFrame.Position = UDim2.new(0.5, -100, 0.5, -50)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)

local Btn = Instance.new("TextButton", MainFrame)
Btn.Size = UDim2.new(1, -20, 1, -20)
Btn.Position = UDim2.new(0, 10, 0, 10)
Btn.Text = "SIEU AUTO ATTACK: OFF"
Btn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
Btn.TextColor3 = Color3.new(1,1,1)

_G.AutoFarm = false
Btn.MouseButton1Click:Connect(function()
    _G.AutoFarm = not _G.AutoFarm
    Btn.Text = _G.AutoFarm and "AUTO ATTACK: ON" or "AUTO ATTACK: OFF"
    Btn.BackgroundColor3 = _G.AutoFarm and Color3.fromRGB(0, 180, 0) or Color3.fromRGB(50, 50, 50)
    
    if _G.AutoFarm then
        task.spawn(function()
            while _G.AutoFarm do
                pcall(function()
                    EquipWeapon() -- Cầm vũ khí
                    
                    -- LỆNH REGISTERHIT BẠN VỪA TÌM
                    -- Tìm bất kỳ con Bandit nào ở gần để đánh vào RightUpperLeg
                    for _, v in pairs(game.Workspace.Enemies:GetChildren()) do
                        if v.Name == "Bandit" and v:FindFirstChild("RightUpperLeg") then
                            local args = {
                                [1] = v.RightUpperLeg, -- Đánh vào chân phải quái
                                [2] = {},
                                [4] = "058e7a71" -- MÃ BẢO MẬT QUAN TRỌNG
                            }
                            game:GetService("ReplicatedStorage"):WaitForChild("Modules"):WaitForChild("Net"):WaitForChild("RE/RegisterHit"):FireServer(unpack(args))
                        end
                    end
                end)
                task.wait(0.1) -- Tốc độ cực nhanh
            end
        end)
    end
end)
