-- [[ GEMINI HUB - VERSION 5.0 COMPACT ]] --
local Player = game.Players.LocalPlayer
local Character = Player.Character or Player.CharacterAdded:Wait()
local Humanoid = Character:WaitForChild("Humanoid")

-- 1. GIAO DIỆN (SCREEN GUI)
local ScreenGui = Instance.new("ScreenGui", game.CoreGui)
ScreenGui.Name = "GeminiV5"

-- NÚT NHỎ ĐỂ MỞ/ĐÓNG (Nằm ở góc trái hoặc bạn có thể kéo đi chỗ khác)
local OpenBtn = Instance.new("TextButton", ScreenGui)
OpenBtn.Size = UDim2.new(0, 50, 0, 30)
OpenBtn.Position = UDim2.new(0, 10, 0, 150)
OpenBtn.Text = "OPEN"
OpenBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
OpenBtn.TextColor3 = Color3.new(1,1,1)
OpenBtn.Draggable = true -- Bạn có thể kéo nút này đi bất cứ đâu cho đỡ vướng

-- KHUNG MENU CHÍNH
local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 200, 0, 160)
MainFrame.Position = UDim2.new(0.5, -100, 0.5, -80)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
MainFrame.Visible = false -- Mặc định là ẩn đi cho đỡ vướng
MainFrame.Active = true
MainFrame.Draggable = true

local UIListLayout = Instance.new("UIListLayout", MainFrame)
UIListLayout.Padding = UDim.new(0, 5)
UIListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

-- Tiêu đề Menu
local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 30)
Title.Text = "GEMINI V5"
Title.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
Title.TextColor3 = Color3.new(1,1,1)

-- NÚT AUTO FARM
local AttackBtn = Instance.new("TextButton", MainFrame)
AttackBtn.Size = UDim2.new(0, 180, 0, 45)
AttackBtn.Text = "AUTO FARM: OFF"
AttackBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
AttackBtn.TextColor3 = Color3.new(1,1,1)

-- NÚT SPEED
local SpeedBtn = Instance.new("TextButton", MainFrame)
SpeedBtn.Size = UDim2.new(0, 180, 0, 45)
SpeedBtn.Text = "SPEED: OFF"
SpeedBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
SpeedBtn.TextColor3 = Color3.new(1,1,1)

-- CHỨC NĂNG ĐÓNG/MỞ
OpenBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
    OpenBtn.Text = MainFrame.Visible and "CLOSE" or "OPEN"
end)

-- LOGIC AUTO FARM (Dùng mã bảo mật bạn vừa lấy)
_G.AutoFarm = false
AttackBtn.MouseButton1Click:Connect(function()
    _G.AutoFarm = not _G.AutoFarm
    AttackBtn.Text = _G.AutoFarm and "AUTO FARM: ON" or "AUTO FARM: OFF"
    AttackBtn.BackgroundColor3 = _G.AutoFarm and Color3.fromRGB(0, 150, 0) or Color3.fromRGB(60, 60, 60)
    
    if _G.AutoFarm then
        task.spawn(function()
            while _G.AutoFarm do
                pcall(function()
                    -- Luôn cầm vũ khí
                    local tool = Player.Backpack:FindFirstChild("Combat") or Character:FindFirstChild("Combat")
                    if tool and not Character:FindFirstChild(tool.Name) then Humanoid:EquipTool(tool) end
                    
                    -- Combo Chống Văng
                    game:GetService("ReplicatedStorage").Modules.Net["RE/RegisterAttack"]:FireServer(0.5)
                    
                    for _, v in pairs(game.Workspace.Enemies:GetChildren()) do
                        if v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 then
                            if (v.Humanoid
